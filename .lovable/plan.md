

# Remove Overlay Text from Instagram Models Video Banner

Remove all overlay text and gradient from `InstagramModelsBanner.tsx`, keeping only the clean video with rounded corners.

## Changes
**`src/components/InstagramModelsBanner.tsx`**
- Remove the dark gradient overlay div
- Remove the text overlay div (OGURA FASHION, INSTAGRAM MODELS, subtext)
- Keep only the video element inside the rounded container

